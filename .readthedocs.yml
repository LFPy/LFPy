version: 2

sphinx:
  configuration: doc/conf.py

formats: all

build:
  image: latest

conda:
  environment: doc/environment.yml

python:
    version: 3
    install:
        - method: pip
          path: .
